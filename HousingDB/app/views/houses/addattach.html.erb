<%= nested_form_for(@house, :html => {:class => "form-horizontal"}) do |f| %>
<fieldset>
<%= render('housefile_table', :f => f) %>

<%=link_to_add_fields("Add a File",f, :housefiles, class: 'btn btn-primary')%><br /><br /><br />
</fieldset>
<%= f.submit :class => 'btn btn-primary' %>
<% end %>
